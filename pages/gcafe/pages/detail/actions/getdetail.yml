id: '16967366657880.9306383844761676'
name: getdetail
nameConfigured: true
steps:
  - id: '16967366747160.12406538404808387'
    type: customSQLQuery
    name: query
    params:
      - type: query
        value: |
          SELECT 
              cafe_cafe_tab.id,
              cafe_cafe_tab.cafe_name,
              cafe_cafe_tab.number_of_pc as Size,
              authkey,
              cafe_cafe_tab.currentstatus as status_code,
              cafe_url,
              cafe_phone,
              contact_name,
              contact_email,
              contact_phone,
              address,
              CONCAT(
                  '[',
                  GROUP_CONCAT(
                      CONCAT(
                          '{',
                          '\"Pkg Name\":\"', product_item_tab.name, '\",',
                          '\"Exp\":\"', product_cafe_product_tab.end_date, '\",',
                          '\"Purchase date\":\"', product_cafe_product_tab.update_time, '\",',
                          '\"Start Date\":\"', product_cafe_product_tab.start_date, '\"',
                          '}'
                      )
                      SEPARATOR ','
                  ),
                  ']'
              ) AS package_info,
              cafe_cafe_tab.country_id,
              cafe_cafe_tab.district_id,
              cafe_cafe_tab.area_id,
              cafe_cafe_tab.region_id,    
              gcmsbase_district_tab.districtname,
              gcmsbase_region_tab.regionname,
              gcmsbase_area_tab.areaname,
              gcmsbase_country_tab.countryname
          FROM cafe_cafe_tab
          LEFT JOIN gcmsbase_country_tab 
              ON gcmsbase_country_tab.id = cafe_cafe_tab.country_id 
          LEFT JOIN gcmsbase_district_tab 
              ON gcmsbase_district_tab.id = cafe_cafe_tab.district_id 
          LEFT JOIN gcmsbase_area_tab 
              ON gcmsbase_area_tab.id = cafe_cafe_tab.area_id
          LEFT JOIN gcmsbase_region_tab 
              ON gcmsbase_region_tab.id = cafe_cafe_tab.region_id
          LEFT JOIN product_cafe_product_tab 
              ON product_cafe_product_tab.cafe_id = cafe_cafe_tab.id 
          LEFT JOIN product_item_tab 
              ON product_item_tab.id = product_cafe_product_tab.item_id
          WHERE 
          cafe_cafe_tab.id = {{activeRoute.queryParams.cafeid}}
          GROUP BY cafe_cafe_tab.id
          ORDER by cafe_cafe_tab.id
        valueType: interpolatedValue
        interpolationType: string
      - type: datasourceId
        value: GCMS-SING-VIAProxy
        valueType: custom
        mustSelectFirst: true
      - type: preparedQuery
        value: |
          SELECT 
              cafe_cafe_tab.id,
              cafe_cafe_tab.cafe_name,
              cafe_cafe_tab.number_of_pc as Size,
              authkey,
              cafe_cafe_tab.currentstatus as status_code,
              cafe_url,
              cafe_phone,
              contact_name,
              contact_email,
              contact_phone,
              address,
              CONCAT(
                  '[',
                  GROUP_CONCAT(
                      CONCAT(
                          '{',
                          '\\"Pkg Name\\":\\"', product_item_tab.name, '\\",',
                          '\\"Exp\\":\\"', product_cafe_product_tab.end_date, '\\",',
                          '\\"Purchase date\\":\\"', product_cafe_product_tab.update_time, '\\",',
                          '\\"Start Date\\":\\"', product_cafe_product_tab.start_date, '\\"',
                          '}'
                      )
                      SEPARATOR ','
                  ),
                  ']'
              ) AS package_info,
              cafe_cafe_tab.country_id,
              cafe_cafe_tab.district_id,
              cafe_cafe_tab.area_id,
              cafe_cafe_tab.region_id,    
              gcmsbase_district_tab.districtname,
              gcmsbase_region_tab.regionname,
              gcmsbase_area_tab.areaname,
              gcmsbase_country_tab.countryname
          FROM cafe_cafe_tab
          LEFT JOIN gcmsbase_country_tab 
              ON gcmsbase_country_tab.id = cafe_cafe_tab.country_id 
          LEFT JOIN gcmsbase_district_tab 
              ON gcmsbase_district_tab.id = cafe_cafe_tab.district_id 
          LEFT JOIN gcmsbase_area_tab 
              ON gcmsbase_area_tab.id = cafe_cafe_tab.area_id
          LEFT JOIN gcmsbase_region_tab 
              ON gcmsbase_region_tab.id = cafe_cafe_tab.region_id
          LEFT JOIN product_cafe_product_tab 
              ON product_cafe_product_tab.cafe_id = cafe_cafe_tab.id 
          LEFT JOIN product_item_tab 
              ON product_item_tab.id = product_cafe_product_tab.item_id
          WHERE 
          cafe_cafe_tab.id = ?
          GROUP BY cafe_cafe_tab.id
          ORDER by cafe_cafe_tab.id
        valueType: interpolatedValue
      - type: preparedQueryParams
        value:
          - __UI_BAKERY__FORMAT_IF_DATE__(activeRoute.queryParams.cafeid)
        valueType: custom
      - type: customDatabaseName
        value: ''
        valueType: interpolatedValue
      - type: stepVersion
        value: 2
        valueType: custom
      - type: successMapper
        value: return data
        valueType: interpolatedValue
      - type: failureMapper
        value: ''
        valueType: interpolatedValue
      - type: mapperEnabled
        value: true
        valueType: custom
    level: 0
    fallthroughError: false
    isChanged: true
singleStep: false
mode: auto
modeConfigured: true
parentPageId: '16967363346630.07488220613504293'
folder: false
config:
  shouldConfirm: false
  confirmMessage: Are you sure you'd like to proceed with "{{actions.getdetail.name}}"?
  showSuccessToast: false
  successToast: Action "{{actions.getdetail.name}}" successfully executed
  successToastDuration: '3000'
  successToastHideByClick: false
  showErrorToast: true
  errorToast: Action "{{actions.getdetail.name}}" has failed
  errorToastDuration: '3000'
  errorToastHideByClick: false
  delay: '0'
  runWhenFirstReferenced: true
  preserveStateOnPageChange: false
assigned: false
selected: true
configured: true
index: 5
